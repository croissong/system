---
- name: golang
  tags: ['golang']
  become_user: "{{ user.name }}"
  block:

    - name: install gocode
      command: go get -u github.com/stamblerre/gocode

    - name: install shfmt
      command: go get -u mvdan.cc/sh/cmd/shfmt
