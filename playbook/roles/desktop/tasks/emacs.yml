---
- name: emacs
  tags: ['emacs', 'dotfiles']
  become_user: "{{ user.name }}"
  block:

    - name: stow emacs dotfiles
      stow: package=emacs

    - name: install emacs
      import_role: name=pkgs tasks_from=util__install_packages
      vars:
        packages:
          - emacs

    - name: enable emacs systemd user service
      service: name=emacs scope=user enabled=yes state=started
